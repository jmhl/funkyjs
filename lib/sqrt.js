'use strict';

module.exports = sqrt;

/**
 * Returns the square root of a value.
 *
 * @param {number} a
 *
 * @returns {number}
 *
 * @example
 * sqrt(4);
 * // returns 2
 */

function sqrt(a) {
  return Math.sqrt(a);
}

