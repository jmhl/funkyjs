'use strict';

module.exports = square;

/**
 * Squares a given number.
 *
 * @param {number} a
 *
 * @returns {number}
 *
 * @example
 * square(2);
 * // returns 4
 */

function square(a) {
  return a * a;
}
